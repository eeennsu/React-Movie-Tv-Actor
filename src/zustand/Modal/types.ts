export interface LoginModalStoreType {
    isLoginModalOpen: boolean;
    setIsLoginModalOpen: (trigger: boolean) => void;
}

export interface SignUpModalStoreType {
    isSignUpModalOpen: boolean;
    setIsSignUpModalOpen: (trigger: boolean) => void;
}