import type { FC } from 'react';

const MainTitle: FC = () => {
    
    return (
        <span className='text-2xl font-bold text-teal-500'>
            StarPulse
        </span>
    );
};

export default MainTitle;